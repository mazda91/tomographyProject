Question 1)
    _define following classes : *framework

                                *circle
                                    attributes : center / radius / intensity (constant for now)

                                    methods : isInCircle(point)
                                                    return bool
                                                
                                              lengthLineIntersection(list)
                                                    return float
  _define following functions :
                                line(phi,s) return list
                                                    

